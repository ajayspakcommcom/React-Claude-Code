# What We Did — Session Log

---

## Project Setup
- Cloned repo from: https://github.com/ajayspakcommcom/Claude-Code.git
- Working directory: `/Users/spakcomm-ajay/Documents/Claude-Code`
- React app lives at: `React-App/`
- Stack: React 18 + TypeScript 5 + Webpack 5
- Run: `cd React-App && npm start` → http://localhost:3000

---

## Folder Structure (current)

```
Claude-Code/
├── React-App/                        ← Main learning app (Webpack)
│   ├── documentation/
│   │   ├── react-roadmap.md          ← Full roadmap Beginner → Expert
│   │   └── what-we-did.md            ← This file
│   └── src/
│       ├── App.tsx                   ← Renders all demos in sections
│       ├── beginner/
│       │   ├── react-basics/         ← 6 files (JSX → Forms)
│       │   ├── hooks/                ← 15 built-in hooks + custom/
│       │   │   └── custom/           ← 8 custom hooks
│       │   └── practice/             ← Counter, Todo, CRUD apps
│       └── intermediate/
│           └── advanced-react/       ← 5 files (Controlled → Context)
│
└── React-App-FileRouter/             ← Standalone routing app (Vite)
    ├── vite.config.ts                ← Uses @tanstack/router-plugin
    └── src/
        ├── main.tsx                  ← Router config (context, scrollRestoration, pendingMs)
        ├── routeTree.gen.ts          ← AUTO-GENERATED by plugin (never edit)
        ├── auth.ts                   ← Mock auth store
        ├── components/
        │   └── HeavyPage.tsx         ← Lazy-loaded component (separate chunk)
        └── routes/
            ├── __root.tsx            ← Root layout + createRootRouteWithContext + activeOptions
            ├── index.tsx             ← / (home, file→route mapping table)
            ├── about.tsx             ← /about
            ├── login.tsx             ← /login (useNavigate, useSearch for redirect)
            ├── contact.tsx           ← /contact (useNavigate full demo)
            ├── lazy-demo.tsx         ← /lazy-demo (lazyRouteComponent, code splitting)
            ├── users/
            │   ├── index.tsx         ← /users (pendingComponent, staleTime)
            │   └── $userId.tsx       ← /users/:userId (useParams nested, errorComponent)
            ├── products/
            │   ├── index.tsx         ← /products (useSearch, loaderDeps, activeOptions, masking, context)
            │   └── $productId.tsx    ← /products/:id (parallel loaders, useParams nested)
            └── _auth/
                ├── _auth.tsx         ← Pathless layout (beforeLoad guard)
                └── dashboard.tsx     ← /dashboard (protected)
```

---

## Roadmap Progress

### ✅ BEGINNER (Complete)

#### React Basics → `src/beginner/react-basics/`
| File | Topic |
|------|-------|
| 01_JSX.tsx | JSX expressions, className, inline styles |
| 02_FunctionalComponents.tsx | Defining & composing components |
| 03_PropsVsState.tsx | Props (read-only) vs State (triggers re-render) |
| 04_EventsAndConditionalRendering.tsx | onClick/onChange + ternary/&& rendering |
| 05_ListsAndKeys.tsx | Array.map(), key prop, avoid index keys |
| 06_BasicForms.tsx | Controlled inputs, handleChange pattern |

#### Hooks → `src/beginner/hooks/`
| File | Topic |
|------|-------|
| 01_useState.tsx | Number, functional update, string, object, array state |
| 02_useEffect.tsx | Mount, deps, API fetch, timer, cleanup |
| 03_useRef.tsx | DOM access, persist without re-render, interval ID |
| 04_useMemo.tsx | Expensive calc, filter large list, stable object ref |
| 05_useCallback.tsx | Stable function ref, with deps, in lists |
| 06_useContext.tsx | Theme, auth context, avoid prop drilling |
| 07_useReducer.tsx | Counter, todo list, form state |
| 08_useLayoutEffect.tsx | DOM measure, tooltip, firing order |
| 09_useImperativeHandle.tsx | forwardRef, custom input, modal |
| 10_useId.tsx | Label linking, ARIA attributes |
| 11_useTransition.tsx | Filter 10k items, slow tab switching |
| 12_useDeferredValue.tsx | Search list, deferred slider |
| 13_useDebugValue.tsx | DevTools labels on custom hooks |
| 14_useSyncExternalStore.tsx | Online status, window size, custom store |
| 15_useInsertionEffect.tsx | CSS-in-JS injection, firing order |

#### Custom Hooks → `src/beginner/hooks/custom/`
| File | Topic |
|------|-------|
| 01_useFetch.tsx | Fetch with loading/error/refetch, AbortController |
| 02_useLocalStorage.tsx | useState synced to localStorage + cross-tab |
| 03_useDebounce.tsx | Delay value update (search-as-you-type) |
| 04_useWindowSize.tsx | Track window width/height + breakpoint |
| 05_usePrevious.tsx | Remember previous render value |
| 06_useClickOutside.tsx | Click outside dropdown/modal/popover |
| 07_useToggle.tsx | Boolean on/off with toggle/setTrue/setFalse |
| 08_useForm.tsx | Form state, validation, touched, submit, reset |

#### Practice → `src/beginner/practice/`
| File | What it demonstrates |
|------|---------------------|
| 01_Counter.tsx | useState, events, conditional rendering, step selector |
| 02_TodoApp.tsx | useReducer, filter tabs, inline edit (double-click), clear completed |
| 03_CRUDApp.tsx | Full Create/Read/Update/Delete, inline edit row, search, grade badges |

#### Tooling — No files created (already in use)
- Node.js, npm, Webpack, Git — all used in this project

---

### ✅ INTERMEDIATE — Advanced React (Complete)

All files in `src/intermediate/advanced-react/`:

| File | Topic | Key concepts |
|------|-------|-------------|
| 01_ControlledVsUncontrolled.tsx | Controlled vs Uncontrolled | state-driven vs ref-driven, file input |
| 02_LiftingStateUp.tsx | Lifting State Up | temp converter, shopping cart, accordion |
| 03_CompositionPatterns.tsx | Composition Patterns | children prop, specialisation, slot, wrapper, compound components |
| 04_Refs.tsx | Refs (Advanced) | forwardRef, callback ref, instance cache, combined ref |
| 05_ContextAPI.tsx | Context API | Context+Reducer, multiple contexts, split state/dispatch for performance |

---

### ✅ INTERMEDIATE — Routing — File-based (Complete)

**Decision**: Used TanStack Router (more powerful, 100% type-safe) over React Router.
**Stack**: TanStack Router v1.114 + Vite + TypeScript, in `React-App-FileRouter/`

#### Run file-based routing app:
```bash
cd React-App-FileRouter
npm run dev        # → http://localhost:5173
```

#### Session 1 — Core file-based routing

| Concept | File | What it shows |
|---------|------|--------------|
| File → Route mapping | All route files | `about.tsx` = `/about`, `$userId.tsx` = `/:userId` |
| Root layout | `__root.tsx` | nav + `<Outlet />`, wraps every page |
| Index route | `index.tsx` | matches exact folder path |
| Nested routes | `users/index.tsx` + `$userId.tsx` | parent list + child detail side by side |
| Dynamic params | `$userId.tsx` | `params.userId` — fully typed at compile time |
| Route loader | `users/index.tsx`, `$userId.tsx` | fetch before render, `useLoaderData()` |
| Pathless layout | `_auth.tsx` | groups protected routes, no URL segment added |
| Protected routes | `_auth.tsx` beforeLoad | `redirect` to `/login` if not logged in |
| Search params | `login.tsx` validateSearch | `z.object({ redirect: z.string() })` — typed |
| Auto-generated tree | `routeTree.gen.ts` | Vite plugin watches `src/routes/`, never edit manually |

#### Session 2 — Remaining concepts (this session)

| Concept | File | What it shows |
|---------|------|--------------|
| `pendingComponent` | `users/index.tsx`, `users/$userId.tsx`, `products/`, `lazy-demo.tsx` | spinner shown when loader takes > `defaultPendingMs` (500ms) |
| `errorComponent` | `users/$userId.tsx`, `products/$productId.tsx` | per-route error boundary — only that route's UI is replaced |
| `staleTime` | `users/index.tsx` (10s), `products/index.tsx` (15s) | cache loader result — revisiting within window skips refetch |
| `loaderDeps` | `products/index.tsx` | re-run loader when search params change |
| `useSearch` (typed) | `products/index.tsx` | Zod `validateSearch` → `useSearch()` → fully typed `{ category, sort }` |
| `useNavigate` | `contact.tsx` | `navigate({ to, search, params, replace })` — all variants with demo buttons |
| `useParams` (nested) | `users/$userId.tsx` → `UserBadge`, `products/$productId.tsx` → `ProductBreadcrumb` | read typed params from a child component deep in the tree — no prop drilling |
| `Link activeOptions` | `__root.tsx` (exact on Home), `products/index.tsx` (includeSearch on category tabs) | `exact: true` prevents `/` matching every route; `includeSearch: true` matches only when search params also match |
| Lazy routes | `lazy-demo.tsx` + `components/HeavyPage.tsx` | `lazyRouteComponent(() => import(...))` — separate JS chunk, only downloaded on first visit |
| Scroll restoration | `main.tsx` | `scrollRestoration: true` — browser remembers scroll position on back/forward |
| `defaultPendingMs` | `main.tsx` | 500ms threshold before showing pendingComponent (avoids flash on fast responses) |
| `defaultPendingMinMs` | `main.tsx` | 300ms minimum — keeps spinner visible long enough to avoid jarring flicker |
| Route masking | `products/index.tsx` | `<Link mask={{ to: '/products' }}>` — real route renders, browser URL shows masked URL |
| Parallel loaders | `products/$productId.tsx` | `Promise.all([fetchProduct, fetchReviews])` — both fetches run simultaneously |
| Context in loaders | `main.tsx` (context: { theme }), `__root.tsx` (createRootRouteWithContext), `products/index.tsx` | typed context flows from `createRouter` → every loader's `context` argument |

#### Key decisions / gotchas learned this session
- `useNavigate` with a reducer `search: (prev) => ...` gets typed as `never` if you don't scope it with `to` — always include `to` when updating search params so the router knows which route's schema to use.
- `createRootRouteWithContext<RouterContext>()` (not `createRootRoute()`) is required for `context` in loaders to be typed — changing `__root.tsx` is the correct approach.
- `loaderDeps` is required when search params should trigger a loader re-run — without it, changing the URL search params does nothing to the loader.
- `lazyRouteComponent` build output: `HeavyPage-xxxx.js` appears as a separate chunk in Vite's build output — confirms code splitting is working.

---

---

### ✅ INTERMEDIATE — Routing — Code-based (Complete)

**Project**: `React-App-CodeRouter/` (Vite, same port as FileRouter — run separately)

#### What is code-based routing?
Instead of using a file/folder naming convention + Vite plugin, you define every route manually in TypeScript and wire the tree together yourself.

#### Run code-based routing app:
```bash
cd React-App-CodeRouter
npm run dev        # → http://localhost:5173
```

#### Key difference from file-based routing

| Task | File-based | Code-based |
|------|-----------|------------|
| Add a route | Create `src/routes/about.tsx` | `createRoute({ path: '/about' })` in `router.tsx` |
| Dynamic param | Name file `$userId.tsx` | `path: '$userId'` in `createRoute()` |
| Nested routes | Put files in a folder | `route.addChildren([childRoute])` |
| Root layout | Create `__root.tsx` | `createRootRoute({ component: Layout })` |
| Route tree | Auto-generated by Vite plugin | Assembled manually with `addChildren()` |
| No code generation | ❌ needs `routeTree.gen.ts` | ✅ pure TypeScript |
| Vite plugin | Required | Not needed |

#### Concepts demonstrated in React-App-CodeRouter

| Concept | File | Notes |
|---------|------|-------|
| `createRootRouteWithContext<T>()` | `router.tsx` | Root layout + typed context for all loaders |
| `createRoute()` | `router.tsx` | Every route defined explicitly with `getParentRoute` |
| `addChildren()` | `router.tsx` | Nesting: `usersRoute.addChildren([userDetailRoute])` |
| `createRouter()` + `context` | `router.tsx` | Assembles tree + passes `{ theme: 'light' }` to all loaders |
| `getRouteApi()` | All page files | Access typed route hooks without circular deps |
| `beforeLoad` guard | `router.tsx` (dashboardRoute) | Same API as file-based — redirect to login |
| `loader` + `staleTime` | `router.tsx` (usersRoute, productsRoute) | Fetch before render, cache result |
| `pendingComponent` | `router.tsx` (multiple routes) | Spinner while loader runs |
| `errorComponent` | `router.tsx` (userDetailRoute, productDetailRoute) | Per-route error boundary |
| `validateSearch` + `loaderDeps` | `router.tsx` (productsRoute) | Typed search params with Zod |
| Context in loaders | `router.tsx` (productsRoute loader) | `context.theme` — typed via `createRootRouteWithContext` |
| Parallel loaders | `router.tsx` (productDetailRoute) | `Promise.all([fetchProduct, fetchReviews])` |
| Route masking | `pages/Products.tsx` | `<Link mask={{ to: '/products', search: {...} }}>` |
| Lazy routes | `router.tsx` (lazyRoute) + `pages/LazyPage.tsx` | `lazyRouteComponent(() => import(...))` — separate chunk confirmed in build |
| `useSearch` | `pages/Products.tsx` | `routeApi.useSearch()` — typed category + sort |
| `useNavigate` | `pages/Contact.tsx` | All variants: to, search, params, replace |
| `useParams` (nested) | `pages/UserDetail.tsx`, `pages/ProductDetail.tsx` | `useParams({ from })` in child component |
| `Link activeOptions` | `pages/Products.tsx` | `exact + includeSearch` on category tabs |
| `scrollRestoration` + `defaultPendingMs` | `router.tsx` (createRouter) | Same config options as file-based |

#### Gotchas learned
- `getRouteApi('/path')` solves the circular dependency: page files can't import the route object from `router.tsx` (which imports the page) — `getRouteApi()` looks up by path string at runtime, fully typed.
- `createRootRouteWithContext<T>()` uses **double-call** syntax: `createRootRouteWithContext<RouterContext>()({ component: ... })` — the first call sets the type, the second call sets the options.
- `lazyRouteComponent` build output: `LazyPage-xxxx.js` is a separate chunk — confirmed in `npm run build` output.

---

### ✅ INTERMEDIATE — State Management (Complete)

All files in `src/intermediate/state-management/`:

---

#### 01_ContextReducer.tsx — Context + useReducer (no library)

**Demo:** Shopping cart (add, remove, increment, decrement, clear)

| Concept | Detail |
|---------|--------|
| Discriminated union | `CartAction` type — TS narrows each `case` in the switch |
| Pure reducer | `cartReducer(state, action) → newState` — zero side effects, fully testable |
| Split contexts | `CartStateCtx` + `CartDispatchCtx` — dispatch-only components never re-render on state change |
| `CartProvider` | Single `useReducer` call, provides both contexts |
| `useCart()` | Reads state + derives `totalItems`, `totalPrice` |
| `useCartDispatch()` | Dispatch only — components using this never re-render when cart changes |
| `memo()` | `ProductList` wrapped in `memo` + uses dispatch only → proven zero re-renders |

**When to use:** Small-medium apps, 1-3 shared slices, no extra dependencies.

---

#### 02_ReduxToolkit.tsx — Redux Toolkit

**Demo:** Counter + Todo list (two slices in one store)

| Concept | Detail |
|---------|--------|
| `createSlice()` | `name + initialState + reducers` — generates action creators + reducer in one block |
| Immer built-in | Write `state.value += 1` — RTK converts to immutable update under the hood |
| Auto action types | `"counter/increment"`, `"todos/addTodo"` — auto-generated from `name/reducerKey` |
| `configureStore()` | Combines `counter` + `todos` slices into one store |
| `Provider` | Wraps demo — gives all children access to the store |
| `useSelector(s => s.counter)` | Subscribes component, re-renders only when that slice changes |
| `useDispatch()` | Fires actions: `dispatch(increment())`, `dispatch(addTodo("text"))` |
| `RootState` + `AppDispatch` | Inferred from store — no manual type maintenance |

**When to use:** Large apps, many slices, complex async, devtools-heavy teams.

---

#### 03_RTKQuery.tsx — RTK Query (built into Redux Toolkit)

**Demo:** Posts CRUD via JSONPlaceholder API (list, get by ID, add, delete)

| Concept | Detail |
|---------|--------|
| `createApi()` | Defines API service — `reducerPath`, `baseQuery`, `tagTypes`, `endpoints` |
| `fetchBaseQuery()` | Thin fetch wrapper — sets base URL |
| `builder.query<R,A>()` | GET endpoint — auto-fetches, cached by query arg |
| `builder.mutation<R,A>()` | POST/PATCH/DELETE — triggered manually |
| `providesTags` | Marks what a query "owns" in cache |
| `invalidatesTags` | Mutation tells RTK which queries to re-fetch |
| `useGetPostsQuery()` | `{ data, isLoading, isFetching, isError, refetch }` |
| `useAddPostMutation()` | `[trigger, { isLoading, isSuccess }]` |
| `skip` option | Query won't fire until condition is true |
| Store wiring | `api.reducer` + `api.middleware` added to `configureStore` |

**When to use:** Redux-based projects that need data fetching — replaces manual `useEffect` fetch patterns.

---

#### 04_TanStackQuery.tsx — TanStack Query (React Query)

**Demo:** Posts list, single post lookup, parallel user fetch, pagination, add/update/delete, infinite scroll, prefetch

| Concept | Detail |
|---------|--------|
| `QueryClient` + `QueryClientProvider` | Central cache store — created once, wraps the app |
| `useQuery({ queryKey, queryFn })` | Fetch + cache. Key = cache identity. Same key = no duplicate fetch |
| `staleTime` + `gcTime` | `staleTime`: how long data is "fresh". `gcTime`: how long unused cache stays in memory |
| `select` | Transform data inside the query — component re-renders only when selected result changes |
| `enabled` | Conditional fetch — `enabled: userId !== ""` prevents premature calls |
| Dependent queries | `enabled: !!postQ.data` — comments query waits for post query to succeed |
| Parallel queries | Multiple `useQuery` calls in one component — all fire simultaneously |
| `keepPreviousData` | Old page stays visible while next page loads — no blank flash |
| `useMutation` — add | `invalidateQueries(["posts"])` triggers list re-fetch after POST |
| `useMutation` — update | `setQueryData` patches cache directly — no extra network call |
| `useMutation` — delete | Optimistic update: remove from cache instantly, rollback via `onError` |
| `cancelQueries` | Cancels in-flight refetches before optimistic update to avoid race conditions |
| `useInfiniteQuery` | `data.pages` accumulates pages. `getNextPageParam` returns next page arg |
| `prefetchQuery` | Warms cache on hover — click renders instantly with no spinner |
| `useQueryClient()` | Access `invalidateQueries`, `setQueryData`, `getQueryData`, `prefetchQuery` in components |
| `ReactQueryDevtools` | Floating panel — inspect cache, status, staleTime, refetch live |

**When to use:** Any app fetching from REST/GraphQL APIs — the most popular server-state library in the React ecosystem.

---

#### Packages installed
```bash
npm install @reduxjs/toolkit react-redux
npm install @tanstack/react-query @tanstack/react-query-devtools
```
`tsconfig.json` — added `"skipLibCheck": true` (required for react-query-devtools type compatibility).

---

## What's Next

### Remaining Intermediate sections
```
✅ Advanced React
✅ Routing (File-based + Code-based)
✅ State Management
⬜ Forms & Validation  ← NEXT
⬜ Styling
⬜ Performance
⬜ API Integration
⬜ Testing (Basics)
⬜ Practice
```

### Forms & Validation — plan
All work inside `React-App/src/intermediate/forms-validation/`

| File | Concept |
|------|---------|
| `01_ReactHookForm.tsx` | `useForm`, `register`, `handleSubmit`, `formState.errors` |
| `02_ZodValidation.tsx` | `zodResolver`, schema validation, nested objects, custom rules |
| `03_AdvancedPatterns.tsx` | `useFieldArray`, `useWatch`, `Controller`, dynamic forms |

Install needed: `npm install react-hook-form zod @hookform/resolvers`

---

## Git & GitHub
- Remote: https://github.com/ajayspakcommcom/Claude-Code.git
- Branch: `main`
- `.gitignore` excludes: `node_modules/`, `dist/`, `build/`
